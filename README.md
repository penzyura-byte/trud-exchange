# Trud Exchange CRM + bot + Mini App

## Быстрый запуск локально
1. Скопируйте `.env.example` в `.env`.
2. Заполните `BOT_TOKEN`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`.
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
